import { PhonePipesPipe } from './phone-pipes.pipe';

describe('PhonePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PhonePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
